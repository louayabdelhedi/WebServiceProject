{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Field</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Namefield</th>
                <td>{{ entity.nameField }}</td>
            </tr>
            <tr>
                <th>Typefield</th>
                <td>{{ entity.typeField }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('field') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('field_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Model</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Namemodel</th>
                <td>{{ entity.nameModel }}</td>
            </tr>
            <tr>
                <th>Descmodel</th>
                <td>{{ entity.descModel }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('model') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('model_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
